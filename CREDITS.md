# spark-w3m -- credits and licenses

The plugin's own code is MIT (`LICENSE`).

- w3m -- https://w3m.sourceforge.net -- MIT. The pager and browser this
  plugin lives in; its keymap and PIPE_BUF are the whole mechanism.
- spark -- https://github.com/forgewright-ai/spark -- MIT. The verb this
  plugin calls (`spark read`).

Built with Claude.
